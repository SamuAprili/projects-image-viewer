# PPM format Image Viewer 
Program that reads a PPM format image (P3 or P6) and display it on a SDL window. This program works with a macOS and on a retina display; it also suppose that the comment, if present, is located in the second line of the .ppm file. 
